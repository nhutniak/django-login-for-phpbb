from setuptools import setup

PACKAGE = 'sessionprofile'

setup(
        name='sessionprofile',
        version='1.0.2',
        packages=['sessionprofile'],
        package_dir = {'': 'django'},

        platforms=['any'],
)
